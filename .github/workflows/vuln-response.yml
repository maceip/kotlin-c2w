name: Vulnerability Scan
on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  vuln-scan:
    uses: maceip/.github/.github/workflows/vuln-response.yml@main
    with:
      cvss-threshold: 9.0
